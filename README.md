# 🌬️ Wind Intelligence Deep Agent (风电行业深度情报助手)

这是一个基于 **Crawl4AI** 和 **DeepSeek (LLM)** 构建的自动化风电行业情报分析工具。它能够自动扫描行业门户网站，识别新闻列表，深度抓取文章详情，并利用大模型进行分类、摘要和关键词提取，最后生成结构化的分析报告。

## 🌟 核心功能

- **启发式通用爬取**：无需针对每个网站编写特定的解析规则，利用 URL 数字特征和路径深度智能识别新闻链接。
- **深度内容分析**：集成 DeepSeek 大模型，对文章进行“技术”、“政策”、“市场”三个维度的精准分类。
- **自动化报告**：自动生成格式精美的 Markdown 报告，支持按类别分组展示。
- **鲁棒性设计**：支持反爬对抗（User-Agent 伪装）、自动超时处理及暴力正则保底机制。

## 📁 项目结构

- `wind_ai_agent.py`: 主程序，包含 `WindDeepAgent` 类，负责抓取流程控制。
- `wind_schema.py`: 数据模型定义，使用 Pydantic 规范文章和提取结果的格式。

## 🚀 快速开始

### 1. 安装依赖

首先，确保你的 Python 版本 >= 3.9。

```bash
# 安装 Python 依赖
pip install -r requirements.txt

# 安装必要的浏览器内核 (Crawl4AI/Playwright 驱动)
playwright install chromium
